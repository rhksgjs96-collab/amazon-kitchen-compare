# amazon-kitchen-compare

실제 앱은 `amazon-compare/` 아래에 있다 (Next.js + Prisma + Tailwind, 테스트는 Vitest).
루트가 아니라 `amazon-compare/` 에서 npm 명령을 실행한다.

## 토큰 절약 규칙 (모든 세션에 적용)

이 저장소에서 작업할 때는 아래 규칙을 기본값으로 따른다.
결과물의 완성도를 낮추라는 뜻이 아니라, **같은 결과를 더 적은 컨텍스트로** 내라는 뜻이다.

### 1. 탐색 — 읽기 전에 좁힌다
- 파일 전체를 통째로 읽지 않는다. `grep -n` / `rg` 로 위치를 먼저 특정하고,
  `sed -n '120,180p' <file>` 처럼 해당 구간만 읽는다.
- 300줄이 넘는 파일은 전체 읽기 금지. 목차가 필요하면 `grep -n '^\(function\|class\|export\|def\)'` 로 뼈대만 본다.
- 디렉터리 탐색은 `ls -R` 대신 `find . -name '<pattern>'` 또는 `git ls-files | grep`.

### 2. 명령 출력은 항상 자른다
- 빌드·설치·테스트 로그: `... 2>&1 | tail -30`
- 목록성 출력: `... | head -50`
- `git log --oneline -5`, `git diff --stat` 을 먼저 쓰고, 전체 diff 는 정말 필요할 때만.

### 3. 중복 작업 금지
- 이미 읽은 파일이나 이미 확인한 사실을 다시 확인하지 않는다.
- Edit/Write 직후 검증 목적의 재읽기를 하지 않는다 (실패했으면 도구가 에러를 냈다).
- 이미 내린 결정을 다시 논의하지 않는다.

### 4. 서브에이전트는 요청이 있을 때만
- 사용자가 명시적으로 요청하지 않는 한 Agent / 서브에이전트를 띄우지 않는다.
  에이전트는 컨텍스트를 처음부터 다시 쌓기 때문에 가장 비싼 경로다.
- 여러 관점이 필요한 작업이어도 기본은 현재 세션에서 직접 처리한다.

### 5. 응답은 결론부터, 짧게
- 결론 → 근거 순. 서론·요약 반복·자화자찬 금지.
- 방금 수정한 코드를 답변에 통째로 다시 붙여넣지 않는다.
  변경 지점은 `파일경로:줄번호` 로 가리킨다.
- 선택지 나열 대신 추천안 하나를 제시한다.
- 길어지는 산출물(리포트, 대용량 데이터)은 파일로 쓰고 경로만 알린다.

### 6. 독립적인 호출은 한 번에
- 서로 의존하지 않는 도구 호출은 한 응답 안에서 병렬로 보낸다 (왕복 횟수 = 토큰).

### 7. 작업 범위를 스스로 넓히지 않는다
- 요청받은 것만 한다. 리팩터링·정리·테스트 추가는 요청이 있을 때만.
